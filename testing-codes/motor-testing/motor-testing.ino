#define enb 13
#define in4 12
#define in3 11
#define in2 10
#define in1 9
#define ena 8


void setup() {
  pinMode(ena, OUTPUT);
  pinMode(in4, OUTPUT);
  pinMode(in3, OUTPUT);
  pinMode(in2, OUTPUT);
  pinMode(in1, OUTPUT);
}

void loop() {
}